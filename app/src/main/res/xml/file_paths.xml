<?xml version="1.0" encoding="utf-8"?>
<!--
  The only folder the app shares with the system installer: the one holding the
  downloaded updates. Nothing else on the phone is within its reach.
-->
<paths>
    <cache-path
        name="updates"
        path="updates/" />
</paths>
